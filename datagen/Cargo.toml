[package]
name = "datagen"
version = "0.1.0"
edition = "2024"

[dependencies]
games = { git = "https://github.com/kz04px/games", version = "0.1.0" }
faeries = {path = "../faeries"}
protocols = {path = "../protocols"}
